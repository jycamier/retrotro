apiVersion: v1
kind: Service
metadata:
  name: {{ include "retrotro.fullname" . }}-frontend
  labels:
    {{- include "retrotro.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "retrotro.frontend.selectorLabels" . | nindent 4 }}
